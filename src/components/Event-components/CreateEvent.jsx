import React from 'react';

const CreateEvent = ({ securePassInstance }) => {
    return (
        <div>
            <h1>Create Event</h1>
                
        </div>
    );
};

export default CreateEvent;
